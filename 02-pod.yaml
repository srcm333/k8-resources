apiVersion: v1
kind: pod
metadata:
  name: nginx
  labels:
    project: roboshop
    environment: dev
    purpose: poc
spec:
  container:
  - name: # container name
    image: nginx # by default it will get from docker hub